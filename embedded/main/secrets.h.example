/**
 * @file secrets.h.example
 * @brief WiFi credentials template for Smart Agriculture ESP32
 *
 * Copy this file to secrets.h, fill in your credentials, and
 * ensure secrets.h is listed in .gitignore so it is never committed.
 *
 *   cp secrets.h.example secrets.h
 */

#ifndef SECRETS_H
#define SECRETS_H

#define WIFI_SSID     "your_wifi_network_name"
#define WIFI_PASSWORD "your_wifi_password"

#endif /* SECRETS_H */
